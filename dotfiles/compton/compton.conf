# Opacity
opacity-rule = [
    "90:class_g = 'XTerm'",
    "90:class_g *?= 'Rofi'",
];

# Blur
blur-background = true;
blur-method = "kawase";
blur-strength = 10;

# Other
vsync = true;
backend = "glx";
