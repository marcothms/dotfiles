export TERM="xterm-256color"

xrdb -load $HOME/.Xresources

{%@@ if profile == "work" @@%}
/share/bin/isda
~/bin/.keyboard.scm &
xrandr --output HDMI-2 --auto --right-of HDMI-1
exec i3
{%@@ endif @@%}
